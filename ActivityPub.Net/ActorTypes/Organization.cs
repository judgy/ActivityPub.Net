using ActivityPub.Net.CoreTypes;

namespace ActivityPub.Net.ActorTypes
{
    public class Organization : ActivityStreamsObject
    {
        protected Organization(ActivityStream activityStream) : base(activityStream)
        {
        }
    }
}